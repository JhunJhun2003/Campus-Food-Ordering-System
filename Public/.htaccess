<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Campus-Food-Ordering-System/Public/

    # Redirect all requests to index.php if the file does not exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L,QSA]
</IfModule>
